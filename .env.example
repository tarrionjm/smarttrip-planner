# Example environment configuration for SmartTrip Planner

# Server configuration
PORT=3000

# Database configuration (to be finalized in Phase 2)
DB_HOST=localhost
DB_USER=root
DB_PASS=yourpassword
DB_NAME=smarttrip